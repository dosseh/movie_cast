apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.movieService.db.name }}
spec:
  clusterIP: None  # Headless service obligatoire pour StatefulSet
  selector:
    app: {{ .Values.movieService.db.name }}
  ports:
  - port: {{ .Values.movieService.db.port }}
    targetPort: {{ .Values.movieService.db.port }}
