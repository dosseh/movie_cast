apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.castService.name }}-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.castService.storage | default "2Gi" }}
